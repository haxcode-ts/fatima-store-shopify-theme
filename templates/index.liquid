{% comment %}
  Homepage Template
  Save as: templates/index.liquid
{% endcomment %}

<style>
  .hero-section {
    position: relative;
    height: 500px;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: white;
    background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('https://images.unsplash.com/photo-1548625149-fc4a29cf7092?w=1600') center/cover;
  }
  .hero-content { z-index: 1; max-width: 700px; padding: 0 20px; }
  .hero-subtitle { font-size: 12px; letter-spacing: 2px; text-transform: uppercase; margin-bottom: 15px; color: var(--color-primary); }
  .hero-title { font-size: 48px; margin-bottom: 15px; line-height: 1.2; }
  .hero-description { font-size: 16px; margin-bottom: 30px; opacity: 0.9; line-height: 1.6; }
  .hero-buttons { display: flex; gap: 15px; justify-content: center; flex-wrap: wrap; }
  
  .featured-products { padding: 80px 0; background: white; }
  .section-header { text-align: center; margin-bottom: 50px; }
  .section-title { font-size: 36px; margin-bottom: 15px; color: var(--color-secondary); }
  .section-description { font-size: 16px; color: #666; max-width: 600px; margin: 0 auto; }
  
  .products-slider { position: relative; overflow: hidden; margin-top: 50px; }
  .products-track { display: flex; gap: 30px; transition: transform 0.5s ease; }
  .product-card { min-width: calc(25% - 22.5px); background: white; border-radius: 8px; overflow: hidden; text-decoration: none; transition: transform 0.3s, box-shadow 0.3s; box-shadow: 0 2px 8px rgba(0,0,0,0.08); }
  .product-card:hover { transform: translateY(-8px); box-shadow: 0 12px 24px rgba(0,0,0,0.15); }
  .product-image-wrapper { position: relative; overflow: hidden; width: 100%; aspect-ratio: 1; }
  .product-image { width: 100%; height: 100%; object-fit: cover; transition: transform 0.3s; }
  .product-card:hover .product-image { transform: scale(1.05); }
  .product-badge { position: absolute; top: 12px; right: 12px; background: var(--color-primary); color: white; padding: 6px 14px; border-radius: 20px; font-size: 11px; font-weight: bold; letter-spacing: 0.5px; }
  .product-info { padding: 20px; text-align: center; }
  .product-title { font-size: 18px; font-weight: 600; margin-bottom: 8px; color: var(--color-secondary); line-height: 1.3; }
  .product-subtitle { font-size: 13px; color: #999; margin-bottom: 12px; }
  .product-price { font-size: 20px; color: var(--color-primary); font-weight: bold; }
  .slider-controls { display: flex; gap: 10px; justify-content: center; margin-top: 40px; }
  .slider-btn { width: 45px; height: 45px; border: 2px solid var(--color-primary); background: white; color: var(--color-primary); border-radius: 50%; cursor: pointer; transition: all 0.3s; display: flex; align-items: center; justify-content: center; }
  .slider-btn:hover { background: var(--color-primary); color: white; transform: scale(1.05); }
  .slider-btn:disabled { opacity: 0.3; cursor: not-allowed; }
  .slider-btn:disabled:hover { background: white; color: var(--color-primary); transform: scale(1); }
  
  .about-section { padding: 80px 0; background: var(--color-bg-light); }
  .about-content { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: center; }
  .about-image { border-radius: 8px; overflow: hidden; }
  .about-image img { width: 100%; height: 100%; object-fit: cover; }
  .about-text h2 { font-size: 36px; margin-bottom: 20px; color: var(--color-secondary); }
  .about-text p { font-size: 16px; line-height: 1.8; color: #666; margin-bottom: 15px; }
  .about-features { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; margin-top: 30px; }
  .feature-item { text-align: center; }
  .feature-icon { width: 50px; height: 50px; margin: 0 auto 15px; background: var(--color-primary); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; }
  .feature-title { font-size: 14px; font-weight: bold; color: var(--color-secondary); }
  
  .testimonials-section { padding: 80px 0; background: white; }
  .testimonials-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 30px; margin-top: 50px; }
  .testimonial-card { background: var(--color-bg-light); padding: 30px; border-radius: 8px; }
  .testimonial-text { font-size: 15px; line-height: 1.7; margin-bottom: 20px; font-style: italic; }
  .testimonial-author { font-weight: bold; color: var(--color-secondary); }
  .testimonial-location { font-size: 13px; color: #888; }
  
  .cta-section { background: var(--color-secondary); color: white; padding: 80px 0; text-align: center; }
  .cta-content h2 { font-size: 36px; margin-bottom: 15px; }
  .cta-content p { font-size: 16px; margin-bottom: 30px; opacity: 0.9; }
  
  .newsletter-section { padding: 80px 0; background: white; }
  .newsletter-content { max-width: 700px; margin: 0 auto; text-align: center; }
  .newsletter-content h2 { font-size: 36px; margin-bottom: 15px; color: var(--color-secondary); }
  .newsletter-content > p { font-size: 16px; color: #666; margin-bottom: 40px; line-height: 1.8; }
  .newsletter-form { margin-top: 30px; }
  .newsletter-input-wrapper { display: flex; gap: 10px; max-width: 500px; margin: 0 auto 15px; }
  .newsletter-input { flex: 1; padding: 15px 20px; border: 2px solid #ddd; border-radius: 4px; font-size: 15px; transition: border-color 0.3s; }
  .newsletter-input:focus { outline: none; border-color: var(--color-primary); }
  .newsletter-btn { padding: 15px 35px; background: var(--color-primary); color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 15px; font-weight: bold; transition: all 0.3s; white-space: nowrap; }
  .newsletter-btn:hover { background: #B89851; transform: translateY(-2px); }
  .newsletter-note { font-size: 13px; color: #888; margin-top: 10px; }
  
  @media (max-width: 968px) {
    .hero-title { font-size: 32px; }
    .about-content { grid-template-columns: 1fr; }
    .about-features { grid-template-columns: 1fr; }
    .product-card { min-width: calc(50% - 15px); }
  }
  
  @media (max-width: 768px) {
    .newsletter-input-wrapper { flex-direction: column; }
    .newsletter-btn { width: 100%; }
  }
  
  @media (max-width: 640px) {
    .product-card { min-width: 100%; }
  }
</style>

<div class="hero-section">
  <div class="hero-content">
    <p class="hero-subtitle">AUTHENTIC HOLY LAND ARTIFACTS & GIFTS</p>
    <h1 class="hero-title">From Jerusalem, With Blessings</h1>
    <p class="hero-description">
      Each creation carries the soul of Jerusalem — a sacred link between faith, craftsmanship, and devotion.
    </p>
    <div class="hero-buttons">
      <a href="/collections/all" class="btn">Shop Now</a>
      <a href="/pages/about" class="btn btn-secondary">Our Story</a>
    </div>
  </div>
</div>

<section class="featured-products">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">Sacred Treasures from the Holy Land</h2>
      <p class="section-description">
        Handcrafted by Christian artisans in Jerusalem, each piece embodies centuries of devotion and carries a blessing from the sacred soil.
      </p>
    </div>
    
    <div class="products-slider">
      <div class="products-track" id="productsTrack">
        {% for product in collections.all.products limit: 8 %}
          <a href="{{ product.url }}" class="product-card">
            <div class="product-image-wrapper">
              {% if product.featured_image %}
                <img src="{{ product.featured_image | img_url: '500x500' }}" alt="{{ product.title }}" class="product-image">
              {% endif %}
              {% if product.tags contains 'blessed' %}
                <span class="product-badge">Blessed</span>
              {% endif %}
            </div>
            <div class="product-info">
              <h3 class="product-title">{{ product.title }}</h3>
              <p class="product-subtitle">Handcrafted in Jerusalem</p>
              <p class="product-price">{{ product.price | money }}</p>
            </div>
          </a>
        {% endfor %}
      </div>
    </div>

    <div class="slider-controls">
      <button class="slider-btn" id="homePrevBtn" onclick="slideHomeProducts(-1)">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <polyline points="15 18 9 12 15 6"></polyline>
        </svg>
      </button>
      <button class="slider-btn" id="homeNextBtn" onclick="slideHomeProducts(1)">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <polyline points="9 18 15 12 9 6"></polyline>
        </svg>
      </button>
    </div>

    <div style="text-align: center; margin-top: 40px;">
      <a href="/collections/all" class="btn">View All Collections →</a>
    </div>
  </div>
</section>

<section class="about-section">
  <div class="container">
    <div class="about-content">
      <div class="about-image">
        <img src="https://images.unsplash.com/photo-1614935151651-0bea6508db6b?w=800" alt="Artisan crafting">
      </div>
      <div class="about-text">
        <h2>From Jerusalem, With Devotion</h2>
        <p>
          In My God brings you closer to the Holy Land through handcrafted gifts of faith — sourced from Bethlehem, blessed in Jerusalem, and cherished worldwide.
        </p>
        <p>
          Our journey began in the streets of Jerusalem, where the scent of incense and the sound of prayer fill the air. We partner with Christian artisans whose families have crafted religious art for generations.
        </p>
        <p>
          Every piece we offer is a work of devotion — an echo of the Holy Land's sacred beauty, carrying centuries of faith into your daily life.
        </p>
        <a href="/pages/about" class="btn" style="margin-top: 20px;">Read Our Full Story</a>
      </div>
    </div>

    <div class="about-features">
      <div class="feature-item">
        <div class="feature-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M12 22s-8-4.5-8-11.8A8 8 0 0 1 12 2a8 8 0 0 1 8 8.2c0 7.3-8 11.8-8 11.8z"/>
            <circle cx="12" cy="10" r="3"/>
          </svg>
        </div>
        <p class="feature-title">Certified Authentic</p>
      </div>
      <div class="feature-item">
        <div class="feature-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
            <circle cx="12" cy="7" r="4"/>
          </svg>
        </div>
        <p class="feature-title">Handcrafted with Love</p>
      </div>
      <div class="feature-item">
        <div class="feature-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M12 2L2 7l10 5 10-5-10-5z"/>
            <path d="M2 17l10 5 10-5M2 12l10 5 10-5"/>
          </svg>
        </div>
        <p class="feature-title">Blessed in Jerusalem</p>
      </div>
    </div>
  </div>
</section>

<section class="testimonials-section">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">Voices of Faith</h2>
      <p class="section-description">Hear from our community of believers</p>
    </div>
    
    <div class="testimonials-grid">
      <div class="testimonial-card">
        <p class="testimonial-text">
          "The olive wood cross I purchased carries such profound beauty and meaning. You can feel the devotion in every carved detail."
        </p>
        <p class="testimonial-author">Sarah M.</p>
        <p class="testimonial-location">California</p>
      </div>
      
      <div class="testimonial-card">
        <p class="testimonial-text">
          "Our parish ordered rosaries for confirmation gifts. The authenticity and craftsmanship exceeded all expectations."
        </p>
        <p class="testimonial-author">Father John</p>
        <p class="testimonial-location">New York</p>
      </div>
      
      <div class="testimonial-card">
        <p class="testimonial-text">
          "Owning a piece of Jerusalem has deepened my prayer life. Every time I hold my rosary, I feel connected to the Holy Land."
        </p>
        <p class="testimonial-author">Maria L.</p>
        <p class="testimonial-location">Texas</p>
      </div>
    </div>
  </div>
</section>

<section class="cta-section">
  <div class="container">
    <div class="cta-content">
      <h2>Bring the Holy Land into Your Home</h2>
      <p>Each piece is not just made but lived — carrying centuries of prayer, devotion, and the sacred spirit of Jerusalem into your daily life.</p>
      <a href="/collections/all" class="btn">Begin Your Journey</a>
    </div>
  </div>
</section>

<section class="newsletter-section">
  <div class="container">
    <div class="newsletter-content">
      <h2>Receive Blessings and Stories from Jerusalem</h2>
      <p>Join our community and stay connected to the Holy Land through stories of faith, artisan features, and sacred inspiration.</p>
      
      <form class="newsletter-form">
        <div class="newsletter-input-wrapper">
          <input type="email" class="newsletter-input" placeholder="Your email address" required>
          <button type="submit" class="newsletter-btn">Subscribe</button>
        </div>
        <p class="newsletter-note">We respect your privacy. Unsubscribe anytime.</p>
      </form>
    </div>
  </div>
</section>

<script>
  let homeCurrentSlide = 0;
  const homeSlidesToShow = 4;
  
  function slideHomeProducts(direction) {
    const track = document.getElementById('productsTrack');
    const cards = track.querySelectorAll('.product-card');
    const maxSlide = Math.max(0, cards.length - homeSlidesToShow);
    
    homeCurrentSlide = Math.max(0, Math.min(maxSlide, homeCurrentSlide + direction));
    
    const slideWidth = cards[0].offsetWidth + 30;
    track.style.transform = `translateX(-${homeCurrentSlide * slideWidth}px)`;
    
    document.getElementById('homePrevBtn').disabled = homeCurrentSlide === 0;
    document.getElementById('homeNextBtn').disabled = homeCurrentSlide >= maxSlide;
  }
  
  window.addEventListener('load', () => {
    const track = document.getElementById('productsTrack');
    const cards = track.querySelectorAll('.product-card');
    document.getElementById('homePrevBtn').disabled = true;
    document.getElementById('homeNextBtn').disabled = cards.length <= homeSlidesToShow;
  });
</script>
